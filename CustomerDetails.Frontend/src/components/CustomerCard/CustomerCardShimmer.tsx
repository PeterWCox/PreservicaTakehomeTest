import { Shimmer, ThemeProvider, mergeStyles } from "@fluentui/react";
import { Skeleton } from "@mui/material";

export const CustomerCardShimmer = () => {
  return <Skeleton variant="rectangular" height={72} width={300} />;
};
